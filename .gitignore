node_modules/
*.log

# Persistent browser identity. Deleting it triggers a fresh reCAPTCHA, so it
# is kept between runs - but it is machine-local and must never be committed.
.playwright-profile/

# Session log spill. Written by Claude Code when it runs with cwd inside this
# submodule, not by the scraper. Never part of the skill.
z-logs/
